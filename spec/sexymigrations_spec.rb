require 'spec_helper'

describe Sexymigrations do
  it 'has a version number' do
    expect(Sexymigrations::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
